name: Release

on:
  push:
    tags:
      - "version/*"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: jdx/mise-action@v2

      - run: pnpm install

      - run: ./scripts/do-release.js
